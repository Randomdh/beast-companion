{
  "id": "beast-companion",
  "name": "Beast Companion",
  "description": "Personal AI assistant for A Kid Called Beast collectors",
  "version": "0.1.0",
  "author": "AKCB Community",
  "homepage": "https://github.com/akcb/beast-companion",
  "configSchema": {
    "type": "object",
    "properties": {
      "dataSource": {
        "type": "string",
        "enum": ["local", "remote"],
        "default": "remote",
        "description": "Where to fetch scoring data from"
      },
      "dataApiUrl": {
        "type": "string",
        "default": "http://129.158.41.81:3100",
        "description": "Remote data API URL (if dataSource is remote)"
      },
      "localDataPath": {
        "type": "string",
        "description": "Path to local data files (if dataSource is local)"
      },
      "openSeaApiKey": {
        "type": "string",
        "description": "OpenSea API key for listings data"
      },
      "alchemyApiKey": {
        "type": "string",
        "description": "Alchemy API key for chain data"
      },
      "walletAddresses": {
        "type": "array",
        "items": { "type": "string" },
        "default": [],
        "description": "Wallet addresses to track"
      }
    },
    "required": []
  }
}
